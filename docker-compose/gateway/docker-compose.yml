services:
  redis:
    image: redis:7-alpine
    restart: always
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    networks:
      - shared-net
    command: redis-server --appendonly yes

  gateway:
    image: crodion/gateway:latest
    ports:
      - "3000:3000"
    volumes:
      - ./config.json:/app/config.json
    env_file:
      - .env
    environment:
      - REDIS_URL=redis://redis:6379
    depends_on:
      - redis
    networks:
      - shared-net

volumes:
  redis_data:
